import React, { useEffect } from 'react';
import './config/appkit-init'; // Initialize AppKit FIRST
import App from './App';

const AppWrapper: React.FC = () => {
  return <App />;
};

export default AppWrapper;
