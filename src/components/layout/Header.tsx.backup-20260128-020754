import React from 'react';
import { Globe, Wallet, Menu } from 'lucide-react';

const Header = ({ lang, setLang, open, connected, t, scrollToSection }) => {
  return (
    <nav className="fixed top-0 left-0 right-0 z-[100] backdrop-blur-xl border-b border-white/5 bg-slate-950/80">
      <div className="container mx-auto px-6 h-20 flex items-center justify-between">
        <div className="flex items-center gap-3 cursor-pointer" onClick={() => scrollToSection('home')}>
          <img src="/images/logo.png" className="w-10 h-10 object-contain" alt="Logo" />
          <span className="text-2xl font-black tracking-tighter text-white uppercase italic">Loyalty</span>
        </div>
        
        <div className="hidden lg:flex items-center gap-10 text-xs font-black uppercase tracking-[0.2em]">
          <button onClick={() => scrollToSection('about')} className="text-gray-400 hover:text-yellow-500 transition-colors">Project</button>
          <button onClick={() => scrollToSection('portfolio')} className="text-gray-400 hover:text-yellow-500 transition-colors">Portfolio</button>
          <button onClick={() => scrollToSection('roadmap')} className="text-gray-400 hover:text-yellow-500 transition-colors">Roadmap</button>
          <button onClick={() => scrollToSection('vip')} className="text-gray-400 hover:text-yellow-500 transition-colors text-purple-400">VIP</button>
        </div>

        <div className="flex items-center gap-4">
          <button 
            onClick={() => setLang(lang === 'en' ? 'ar' : 'en')}
            className="w-10 h-10 rounded-full bg-white/5 border border-white/10 flex items-center justify-center text-yellow-500 hover:bg-white/10"
          >
            <Globe size={18} />
          </button>
          <button 
            onClick={open}
            className="bg-yellow-500 hover:bg-yellow-400 text-slate-950 px-6 py-2.5 rounded-xl font-black text-sm shadow-[0_0_20px_rgba(251,191,36,0.3)] transition-all flex items-center gap-2"
          >
            <Wallet size={18} /> {connected ? 'CONNECTED' : (lang === 'ar' ? 'ربط المحفظة' : 'CONNECT')}
          </button>
        </div>
      </div>
    </nav>
  );
};

export default Header;
